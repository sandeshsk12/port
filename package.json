{
  "name": "website-scraper",
  "version": "1.0.0",
  "description": "A web scraping tool built with Streamlit",
  "main": "index.js",
  "scripts": {
    "start": "netlify dev",
    "build": "echo \"No build step required\" && exit 0"
  },
  "dependencies": {
    "node-fetch": "^2.6.7",
    "playwright": "^1.42.0",
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@netlify/functions": "^2.4.0",
    "netlify-cli": "^15.0.0",
    "esbuild": "^0.19.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

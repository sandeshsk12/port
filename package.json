{
  "name": "streamlit-netlify-app",
  "version": "1.0.0",
  "description": "Streamlit app deployed on Netlify",
  "main": "netlify/functions/server.js",
  "scripts": {
    "build": "echo 'Build script runs in Netlify'"
  },
  "dependencies": {
    "node-fetch": "^2.6.7",
    "playwright": "^1.42.0",
    "@netlify/functions": "^4.0.0",
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "netlify-cli": "^16.0.0",
    "esbuild": "^0.19.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
